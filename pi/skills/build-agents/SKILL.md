---
name: build-agents
description: DEPRECATED — use the /build-agents command instead.
---

This skill has been replaced by the build-agents extension.
Use `/build-agents` to start a multi-agent build, or `/build` which will offer the choice.
