# ~/bin/copy_to_home
#
# deploys Git repo to current machine's home

# Copy from `home` function
SKELETON="${HOME}/Projects/homedir"

# keep repo up to date
cd ${SKELETON} 
git pull

# sync to my home
rsync -aHvP --exclude .git/ --exclude README.markdown --exclude homedir.tmproj . ~/.