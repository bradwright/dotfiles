# Add JSON, SCSS, and Puppet to ack
--type-add
js=json

--type-set
scss=scss

--type-set
puppet=pp

--type-set
cucumber=feature

--type-set
erb=erb

--type-add
html=html.erb

--type-set
conf=conf

# Ignore SQL files
--nosql

# ignore minified JS files
-G
(?<![\-\.]min\.js)$

# Ignore local .bundle Gems
--ignore-dir=.bundle

# ignore coverage
--ignore-dir=coverage

# always page, and output ansi
--pager=less -r