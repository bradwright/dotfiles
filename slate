# -*- mode: conf-space  -*-

config defaultToCurrentScreen true
config checkDefaultsOnLoad true

# Layout aliases
alias MacBookAir 1440x900

# General aliases
alias sox screenOriginX
alias soy screenOriginY
alias ssx screenSizeX
alias ssy screenSizeY

alias emacsDev move ${sox};${soy} ${ssx};${ssy} ${MacBookAir}

alias mba-full move ${sox};${soy} ${ssx};${ssy} ${MacBookAir}

default home resolutions:${MacBookAir}

# Default for home laptop

# show Emacs in the centre, 100 columns wide in current font
layout home 'Emacs' move (${ssx}-821)/2;0 821;871 ${MacBookAir}
# show Mail right in the centre
layout home 'Mail' move (${ssx}-1150)/2;${soy}+(${ssy}-740)/2 1150;740 ${MacBookAir}
# show iTerm in 140 columns wide, 30 deep
layout home 'iTerm' move (${ssx}-1130)/2;${soy}+(${ssy}-556)/2 1130;556 ${MacBookAir}

# Emacs and iTerm full screen
layout dev 'Emacs' ${mba-full}
layout dev 'iTerm' ${mba-full}

bind h:ctrl;alt;cmd sequence layout home > focus 'Google Chrome'
bind d:ctrl;alt;cmd sequence layout dev > focus 'Emacs'

bind r:ctrl;alt;cmd relaunch